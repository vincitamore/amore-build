{
  "name": "@amore/vinculum",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Typed structural edge derivation for the house graph store (graph/edges.jsonl). Tier-0 emitters only; the iris daemon serves the file.",
  "main": "src/index.ts",
  "scripts": {
    "test": "bun test"
  },
  "dependencies": {
    "@amore/regula": "workspace:*"
  },
  "devDependencies": {
    "@types/node": "latest",
    "bun-types": "latest"
  }
}
