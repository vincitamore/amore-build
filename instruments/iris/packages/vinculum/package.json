{
  "name": "@amore/vinculum",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Typed edge derivation for the house graph store (graph/edges.jsonl). Structural tier-0 plus agentic gen/judge/ingest; the iris daemon serves the file.",
  "main": "src/index.ts",
  "scripts": {
    "test": "bun test"
  },
  "dependencies": {
    "@amore/regula": "workspace:*"
  },
  "devDependencies": {
    "@types/node": "latest",
    "bun-types": "latest"
  }
}
