{
  "name": "@amore/daemon",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "The iris Bun index/read daemon — parses via @amore/regula, serves the org index over HTTP. Golden-master parity with the legacy Rust daemon via @amore/parity.",
  "main": "src/index.ts",
  "scripts": {
    "test": "bun test",
    "start": "bun src/index.ts"
  },
  "dependencies": {
    "@amore/regula": "workspace:*"
  },
  "devDependencies": {
    "@types/node": "latest",
    "bun-types": "latest"
  }
}
