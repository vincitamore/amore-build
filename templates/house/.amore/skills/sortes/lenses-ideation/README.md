# Ideation / discovery catalog

Sibling to `../lenses/` (code-review). **Not** the default pick target.

```bash
bun .amore/skills/sortes/bin/pick.ts --catalog .amore/skills/sortes/lenses-ideation
bun .amore/skills/sortes/bin/pick.ts --catalog .amore/skills/sortes/lenses-ideation --n 5
```

Asset form: `skill://sortes/bin/pick.ts` with `--catalog` pointing at this directory.

Same admission rules as the review catalog: primitive, generalizes, plain-behavioral register, no saved combinations. Prefer growth from `not surfaced:` trails in research campaigns over bulk dumps into default `lenses/`.

## Core slate

Research/discovery primitives (synthesis-ordered highlights; live list is the directory):

1. `range-disjunction` — range resolution paths, not the prior's mode
2. `prior-scope` — closed priors kill shapes, not quests
3. `false-closure` — honest-negative / a-priori theater as exit
4. `construct-the-missing` — absence → construction order
5. `absurd-falsifiable` — weird + kill criterion
6. `transport-vs-essential` — rename ≠ novelty
7. `win-zone-boundary` — name method blindness
8. `foundation-rewrite` — change a prioris when stuck
9. `generative-minimum` — smallest regenerating axiom set
10. `humble-pole` — relocate, don't under-engineer

## Campaign-critical and mature-growth adds

Further lenses admitted from exhibited `not surfaced:` gaps (not bulk dump): `counterexample-first`, `obstacle-fanout`, `multiplicative-bridge`, `exhibit-threshold`, `selection-from-continuum`, `honest-baseline`, `black-box-load`, `warrant-scale`, `invariance-class`, `inherited-partition`, `coordinate-invention`, `transformative-representation`, `solved-analogue`, `obstruction-anatomy`, `deform-to-solvable`, `positivity-certificate`, `nearest-open-rung`, `two-ways-to-count`, `work-backward`, `win-either-way`, `random-model-first`, `average-first`, `find-the-action`, `strengthen-to-prove`, `extremal-reframe`, `local-global-glue`, `equivalent-forms`, `formal-statement-first`, `change-the-engine`.

Admission filter for each: primitive (a single generative move, not a combination), generalizes across substrates, plain-behavioral register, constructive by design (no standing pessimistic lenses that only re-litigate closed priors).

**Still deferred:** design/dream-only set; bulk lattice remaining glyphs; third prose catalog; synonym re-adds.

Default review catalog: growth stays anti-accretion — add only on exhibited gaps.
