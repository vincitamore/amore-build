# Ordered find→replace map for scripts/forbid_strip.py (campaign porting).
# Format: one substitution per non-comment line:
#   find<TAB>replace
#   OR
#   find => replace
#
# Applied in file order (each rule runs fully before the next).
# Only files under templates/ are rewritten.
#
# Prefer {{PLACEHOLDER}} forms so adopters fill house-local names at init.
# IMPORTANT: longer / more-specific finds first; placeholders must not contain
# later find-strings (e.g. avoid {{NCU_…}} when a later rule matches NCU).

# --- full personal paths BEFORE bare name fragments ---
C:\Users\AlexMoyer\Documents\arcus	{{HOUSE_ROOT}}
C:/Users/AlexMoyer/Documents/arcus	{{HOUSE_ROOT}}
C:\Users\AlexMoyer	{{OPERATOR_HOME}}
C:/Users/AlexMoyer	{{OPERATOR_HOME}}
Documents\arcus	{{HOUSE_ROOT_REL}}
Documents/arcus	{{HOUSE_ROOT_REL}}
~/arcus/bin	{{HOUSE_BIN}}

# --- operator / house PII (AlexMoyer before amoyer — amoyer is a substring) ---
AlexMoyer	{{OPERATOR_ID}}
vincitamore	{{OPERATOR_HANDLE}}
amoyer	{{OPERATOR_SHORT}}

# --- generic user-home prefixes (after concrete paths) ---
C:\Users\	{{USER_HOME}}\
C:/Users/	{{USER_HOME}}/

# --- sibling / private house names ---
Materia	{{HOUSE_NAME}}
materia	{{house_name}}
Aether	{{SIBLING_HOUSE}}
aether	{{sibling_house}}
Documents\selene	{{SIBLING_HOUSE_ROOT_REL}}
Documents/selene	{{SIBLING_HOUSE_ROOT_REL}}
~/selene/bin	{{SIBLING_HOUSE_BIN}}
custos	{{house_role}}

# --- private house ports / OMP / dispatch ---
:8765	:{{HOUSE_PORT}}
grok-dispatch	{{DISPATCH_CMD}}
.omp/skills	.arcus/skills
.omp	.arcus

# --- private model / provider footing ---
modal-k3	{{PRIVATE_MODEL_ID}}
selene-k3	{{PRIVATE_MODEL_ID}}

# --- NCU house identifiers (prefix first; placeholders avoid bare NCU/ncu) ---
ncu-	{{HOUSE_UNIT_PREFIX}}-
NCU	{{HOUSE_UNIT}}
ncu	{{house_unit}}

# --- private-house session lore ---
Kimi K3 sessions	{{PRIVATE_SESSION_LABEL}}
